import { TaxClassesPage } from "#web/components/tax-classes-page.tsx";

/** Entrypoint for the main page of the app. */
export function MainPage() {
  return (
    <main>
      <TaxClassesPage />
    </main>
  );
}
